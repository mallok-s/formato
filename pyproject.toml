[project]
name = "formato"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ruff>=0.15.9",
    "ty>=0.0.28",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
